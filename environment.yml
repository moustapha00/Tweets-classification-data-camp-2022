name: ramp-tweets
channels:
  - defaults
  - conda-forge
dependencies:
  - numpy
  - scipy
  - pandas>=0.23
  - scikit-learn>=0.20
  - pyarrow
  - imbalanced-learn
  - matplotlib
  - joblib

  - pip
  - pip:
      - ramp-workflow
      - osfclient
